<?
use Common\ImageService;

/* @var $this Common\View */
/* @var array $photo */
$url = $this->url('video-view', ['alias' => $this->item['alias']]);
?>

<div class="video-entry">
	<a href="<?= $url ?>" style="background-image: url('<?= $this->image($this->item['thumb'], ImageService::SIZE_VIDEO_THUMB) ?>');">
		<span class="title"><?= $this->item['title'] ?></span>
		<? if(!empty($this->item['duration'])) { ?>
		<span class="time"><?= $this->html()->time($this->item['duration']) ?></span>
		<? } ?>
	</a>
</div>
